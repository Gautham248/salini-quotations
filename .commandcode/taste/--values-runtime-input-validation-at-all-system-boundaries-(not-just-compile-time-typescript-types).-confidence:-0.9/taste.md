# - Values runtime input validation at all system boundaries (not just compile-time TypeScript types). Confidence: 0.9
- Values runtime input validation at all system boundaries (not just compile-time TypeScript types). Confidence: 0.9
- Expects invalid/empty data to be rejected at the point of entry rather than silently accepted or deferred. Confidence: 0.85
- Expects comprehensive test coverage for all features — "proper test cases" are a baseline requirement, not optional. Confidence: 0.85
- Prefers defensive input handling: bounds clamping, NaN/Infinity guards, and empty-string rejection as standard practice. Confidence: 0.8
- Prefers scoped, intentional commits — only files directly relevant to the current change are staged; pre-existing dirty files in the working tree are left alone. Confidence: 0.75
- Prefers the Conventional Commits format (`feat:`, `fix:`, etc.) for commit messages with a structured body explaining what changed and why. Confidence: 0.7
- Commits and pushes directly to the `main` branch — no feature-branch or PR workflow expected for this project. Confidence: 0.85
- Treats TypeScript type-checking (`tsc --noEmit`) as a required gate alongside the test suite — type errors are blockers, not warnings. Confidence: 0.8
- When reviewing uncommitted changes, systematically diffs the working tree, reads changed source files, runs the existing test suite as a baseline, then proactively adds tests for any uncovered features. Confidence: 0.75
- When asked specifically for "a commit message" (not "commit this" or "commit and push"), provides only the message text for review — does not assume the full commit+push workflow is desired. The user distinguishes between drafting a message and executing the commit. Confidence: 0.7
- Prefers plan-before-implementation for complex, cross-cutting changes, especially security-sensitive ones — wants to review and approve a plan before code is written. Confidence: 0.8
- Prefers plans to be saved into the project's `docs/plans/` directory as part of the codebase documentation, not only kept in the global tool directory. Confidence: 0.8
- Expects systematic post-implementation adversarial verification for security-sensitive changes (authorization, multi-tenancy, permissions) — grep every data-access path against protected models, confirm each is correctly scoped or deliberately global, and produce a written verification note documenting the analysis. Confidence: 0.85
- Expects adversarial verification notes to include the raw grep/list output (file/line + per-match annotation like SCOPED or GLOBAL), not just a prose summary — the evidence itself must be visible so it can be independently reviewed. Confidence: 0.85
- Expects the agent to flag spec-vs-codebase discrepancies as explicit open questions — when a specification assumes a pattern or mechanism that doesn't actually exist in the codebase, the agent must surface this rather than silently working around it or making assumptions. Confidence: 0.8
- Prohibits running destructive database commands (schema migrations, table drops, backfills) against non-local/remote databases without explicit per-command confirmation — local dev databases are fine for automated migration. Confidence: 0.9
- Expects pre-migration data integrity checks when schema changes introduce or tighten uniqueness constraints — query existing data for constraint violations before applying the migration, and show the raw results (not just an assertion that it passed). Confidence: 0.8
- During verification/review tasks, when a bug is found the agent must stop, explain what's wrong and what the fix would be, then wait for explicit go-ahead before applying any change — never silently fix-and-continue through the rest of the checklist. Confidence: 0.85
- Prefers semantically meaningful atomic writes (e.g., `increment` on a counter column) over no-op updates for database concurrency control — a real state mutation is unambiguous to every database engine's query planner and cannot be optimized away. Confidence: 0.8
- Prefers database-portable concurrency solutions — locking/serialization logic must be correct under both SQLite and Postgres, not rely on engine-specific transaction behavior (e.g., SQLite's DEFERRED→write-lock upgrade). Confidence: 0.8
- Prefers access control at the route/entry-point level (fail-closed: block the entire route) rather than query-level filtering when a user role has no legitimate reason to access a resource — don't just filter results, prevent the call entirely. Confidence: 0.8
- During multi-tenant or security migrations, latent bugs that become real vulnerabilities due to the change are in-scope for the current pass — the agent must not dismiss them as pre-existing and out of scope. Confidence: 0.8
- Prefers raw SQL (`$queryRaw` with `UPDATE ... RETURNING`) over ORM-level operations for atomic counter increments when the ORM/query-engine may decompose the operation into non-atomic SELECT+UPDATE — correctness over abstraction purity. Confidence: 0.75
- Prefers `$queryRaw` (tagged template with parameterized placeholders) over `$queryRawUnsafe` (string interpolation) for any raw SQL — parameterization is the default even when the interpolated values are trusted internal types like `number` from a session; `$queryRawUnsafe` requires explicit justification. Confidence: 0.85
- When deviating from ORM abstractions to raw SQL for correctness reasons, expects the agent to explicitly acknowledge the tradeoff (lost type safety, bypassed schema-driven query builder) and justify why correctness outweighs abstraction purity in that specific case — don't silently default to raw SQL as if it's an obviously superior choice. Confidence: 0.8
- Prefers empirically testing concurrency — when a fix claims to be race-condition-safe, write a throwaway script that calls the function concurrently (e.g., `Promise.all` x10) and asserts all outputs are unique, run it multiple times, then delete the script. Reasoning alone is insufficient. Confidence: 0.85
- When a dependency/abstraction behaves unexpectedly, digs into its internals (e.g., grepping compiled adapter JS for SQL keywords like `BEGIN`) to understand the actual mechanism rather than guessing from documentation or API surface. Confidence: 0.75
- Expects all features from the base branch (`main`) to be preserved when building a feature branch on top — if something from main is missing after the merge, that's a bug to fix, not an intentional removal. "Nothing from the main should be missed in the new branch. Everything that we built regarding [the feature] is to be on top of main." Confidence: 0.85
- When diagnosing feature regressions after a branch merge, expects systematic `git diff main...featurebranch` comparison to identify what was lost rather than ad-hoc investigation — diff the branch to understand the delta, don't guess. Confidence: 0.8
- Role-based permissions are defined as superset-minus-exceptions: "retains all permissions of [existing role] apart from [specific exclusions]." New roles extend/mirror existing guards (e.g., `requireAdmin`, `resolveStoreId`, inline `=== "superadmin"` checks) rather than having completely independent permission sets enumerated from scratch. Confidence: 0.75
- Expects a clear, documented mapping of test credentials to roles as part of the development environment — when asked "give me the login credentials for each role," expects a table with username, password, role, and store association, not just a verbal summary. Confidence: 0.7
- Expects a documented manual regression testing guide as a standard project artifact — a checklist organized by role and feature area (authentication, CRUD modules, multi-tenant isolation, security boundaries), including login credentials, automated test suite reference, and step-by-step manual verification steps with checkboxes. Stored in `docs/` alongside other project documentation. Confidence: 0.8
- Expects management interfaces (stores, users, items, etc.) to expose complete CRUD actions — create, read, edit/update, and toggle/delete — not partial subsets. A management page that only creates but cannot edit is considered incomplete. Confidence: 0.85
- Expects complete tenant isolation: only the superadmin role transcends store boundaries. All other roles (admin, manager, staff) are fully scoped to their assigned store — they can only see and operate on data within that store. No partial cross-store access for any non-superadmin role. Confidence: 0.9
- Expects superadmin interfaces to be cross-store overviews — per-store settings and per-store operations belong in per-store management UIs (modals on the store list, dedicated per-store pages), not mixed into general pages that superadmin navigates to. Superadmin's "bird's eye view" means store-specific editing should never appear on pages like /admin/settings. Confidence: 0.85
- Prohibits browser-native `alert()` / `confirm()` / `window.confirm()` dialogs anywhere in the codebase — all confirmation and notification UI must use proper React components (Dialog, toast/sonner, etc.), never raw browser dialogs. Confidence: 0.9
- Expects audit trail fields (`createdBy`, `updatedBy`) to be correctly populated on all data mutations — deletions, updates, and creations must record who performed the action. The audit trail is a first-class correctness requirement, not an optional enhancement. Confidence: 0.8
- When fixing a specific reported instance of a codebase-wide anti-pattern (e.g., "I found a `confirm()` on this page"), systematically grep the entire codebase for all other occurrences of the same pattern and fix them all in one pass — don't just fix the single instance the user pointed to. Confidence: 0.85
- Prefers extracting reusable UI components (e.g., `ConfirmDialog`) when the same interaction pattern appears in 3+ locations, rather than duplicating dialog/button markup at each call site. The reusable component should accept configuration via props (title, description, confirm label, destructive flag, callbacks). Confidence: 0.8
- Multi-tenant UI components must never contain hardcoded tenant-specific fallback values (e.g., a default company name like "SALINI PALA") — when store-identifying data (company name, address, GSTIN, bank details) fails to load or isn't passed, the fallback must be generic/empty, not a specific real store's data. A hardcoded real-store default is a tenant-isolation data leak: any user whose settings fetch fails will silently see another tenant's information. Confidence: 0.85
- Role-based sidebar navigation should include all links that role needs to complete their workflow, not a minimal subset — if a role needs to browse a shared catalog (master items) while creating quotations, that link must be present in their sidebar, even if the role otherwise has limited access. Confidence: 0.8
- When proxy middleware blocks a role from a broad route prefix (e.g., staff from `/admin/*`), role-appropriate exceptions should be explicit allowlist entries for specific paths (e.g., `pathname === "/admin/items"`) rather than loosening the general prefix block. This keeps the middleware fail-closed: the broad deny is the default, and each exception is a deliberate, reviewable carve-out. Confidence: 0.8
- Prefers defense-in-depth authorization: server-side route/API guards (e.g., `requireAdmin()`) are the primary enforcement layer, but the UI must also hide controls (buttons, toggles, edit icons) that the user's role cannot use — not just for security but for UX clarity. When a role is read-only on a resource, replace interactive controls (Switch toggles) with passive display equivalents (Badge labels) rather than leaving non-functional interactive controls visible. Confidence: 0.8
- When tasked with modifying or replacing a specific file, reads the target file(s) and any explicitly-named reference files in full before writing any code — this is a concrete pre-implementation ritual, not optional skimming. Confidence: 0.75
- Respects task scope boundaries during implementation, not just verification: when a task is explicitly scoped to one layer (e.g., "rendering only, don't touch data/logic"), bugs or issues noticed in other layers must be flagged separately rather than fixed inline. Confidence: 0.7
- When a change introduces new infrastructure dependencies (headless browser binary, new service, new package with deployment-specific constraints), researches deployment compatibility (e.g., serverless function size limits, platform-specific binary support) before implementing and explicitly flags any incompatibilities rather than picking silently. Confidence: 0.75
- When replacing implementation internals (e.g., swapping jsPDF for a different PDF renderer), updates existing tests to match the new internals rather than deleting test coverage — tests should be migrated, not removed. Confidence: 0.7
- PDF/print document templates should share visual design language with the corresponding live browser preview component (same layout, field order, typographic hierarchy) rather than being independently redesigned from scratch. Confidence: 0.65
- When the user provides pre-diagnosed bugs with root causes and specific fixes, implements exactly as specified rather than re-diagnosing from scratch — the user's diagnosis is the spec, not a suggestion. However, does empirically verify each fix by rendering/running the actual output afterward, not just by reading the code. Confidence: 0.8
- Fixes bugs incrementally with empirical verification between each: fix one bug, render/run to confirm it's resolved, then move to the next — does not batch all fixes and check once at the end, especially for visual/layout bugs where a fix might not work as assumed despite looking correct in code. Confidence: 0.8
- Design/aesthetic decisions (color choices, visual styling, accent colors) are surfaced as explicit questions with concrete proposals rather than made silently — the agent proposes options but does not decide unilaterally on matters of visual taste. Confidence: 0.75
- Prefers restrained visual design: accent colors applied sparingly to key text and borders only, not filled backgrounds, not auxiliary text lines (GSTIN, contact info), not table headers. Explicitly specifies both inclusions and exclusions when directing a visual change. Confidence: 0.7
- Prefers show-before-expanding workflow for visual/design changes: apply the minimal agreed-upon change, render and show the result, get explicit approval, then iterate — does not want the agent to extrapolate a design decision to other elements without first demonstrating the effect on the agreed scope. Confidence: 0.7
- Prefers concrete empirical data (measurements, benchmarks, capability demos) over qualitative adjectives when evaluating technical tradeoffs — "get a real number, not just 'no binary' as a qualitative claim," and verify that a proposed library can actually satisfy the specific original requirements before approving it. Confidence: 0.7
- When the agent makes an empirical verification claim (e.g., "only X occurrences found in the output") that turns out to be false, expects the agent to explain the methodological failure that produced the wrong result — not just correct the output and move on. The user needs to understand why the verification was unreliable to decide whether future verification claims using similar methods can be trusted. Confidence: 0.75
- For visual/layout output (PDFs, rendered pages, images), structural analysis of the file's internals (byte streams, content streams, color operators, decompiled markup) is not an acceptable substitute for rendering the artifact to an actual viewable image. The rendered visual is the ground truth; parsing internals is an unreliable proxy that has been proven wrong. Confidence: 0.85
- When a verification method has produced a false result on a specific question, that method is disqualified for subsequent re-verification of the same question — the agent must switch to a qualitatively different, more direct approach (e.g., visual rendering instead of structural parsing) rather than repeating or refining the failed method. Confidence: 0.8
- When the agent makes a diagnostic/root-cause claim ("this is pre-existing", "this is unrelated to X"), expects the agent to show the forensic evidence — git blame, git show, log output, diff — that substantiates the claim, not just state the conclusion. Git history is a standard diagnostic tool for tracing when and how a pattern was introduced; the user wants to see the check, not just the conclusion. Confidence: 0.8
- Prefers pre-task git hygiene: fetches latest main and confirms the working tree is clean and up-to-date before making any changes. If there are uncommitted changes, stops and reports rather than proceeding. Confidence: 0.8
- Prefers proactive layout/rendering stress-testing with realistic extreme datasets (long names, multi-line addresses, long descriptions, 25+ items for pagination, empty optional fields) rather than discovering layout bugs reactively in production. This is a systematic "what's the worst real-world data this could receive?" pass, not just testing with the one reference example that happens to work. Confidence: 0.8
- When a single shared layout/template cannot accommodate extreme-but-realistic data, prefers fixing it with input validation or max-length constraints at the data-entry boundary rather than introducing per-tenant layout branching, per-store template selection, or configuration-driven layout tweaks. Data-entry hygiene is the correct layer for constraining variability; the layout should remain a single shared implementation. Confidence: 0.75
- When layout robustness depends on field-length or format assumptions (e.g., "is companyName bounded to N characters?"), asks about intended constraints rather than silently assuming bounded or unbounded. Does not guess about whether a field has a max length enforced elsewhere in the app. Confidence: 0.75
