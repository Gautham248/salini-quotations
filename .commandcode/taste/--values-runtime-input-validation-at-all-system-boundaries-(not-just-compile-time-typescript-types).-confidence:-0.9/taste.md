# - Values runtime input validation at all system boundaries (not just compile-time TypeScript types). Confidence: 0.9
- Values runtime input validation at all system boundaries (not just compile-time TypeScript types). Confidence: 0.9
- Expects invalid/empty data to be rejected at the point of entry rather than silently accepted or deferred. Confidence: 0.85
- Expects comprehensive test coverage for all features — "proper test cases" are a baseline requirement, not optional. Confidence: 0.85
- Prefers defensive input handling: bounds clamping, NaN/Infinity guards, and empty-string rejection as standard practice. Confidence: 0.8
- Prefers scoped, intentional commits — only files directly relevant to the current change are staged; pre-existing dirty files in the working tree are left alone. Confidence: 0.75
- Prefers the Conventional Commits format (`feat:`, `fix:`, etc.) for commit messages with a structured body explaining what changed and why. Confidence: 0.7
- Commits and pushes directly to the `main` branch — no feature-branch or PR workflow expected for this project. Confidence: 0.85
- Treats TypeScript type-checking (`tsc --noEmit`) as a required gate alongside the test suite — type errors are blockers, not warnings. Confidence: 0.8
- When reviewing uncommitted changes, systematically diffs the working tree, reads changed source files, runs the existing test suite as a baseline, then proactively adds tests for any uncovered features. Confidence: 0.75
- Prefers plan-before-implementation for complex, cross-cutting changes, especially security-sensitive ones — wants to review and approve a plan before code is written. Confidence: 0.7
- Prefers plans to be saved into the project's `docs/plans/` directory as part of the codebase documentation, not only kept in the global tool directory. Confidence: 0.8
- Expects systematic post-implementation adversarial verification for security-sensitive changes (authorization, multi-tenancy, permissions) — grep every data-access path against protected models, confirm each is correctly scoped or deliberately global, and produce a written verification note documenting the analysis. Confidence: 0.85
- Expects adversarial verification notes to include the raw grep/list output (file/line + per-match annotation like SCOPED or GLOBAL), not just a prose summary — the evidence itself must be visible so it can be independently reviewed. Confidence: 0.85
- Expects the agent to flag spec-vs-codebase discrepancies as explicit open questions — when a specification assumes a pattern or mechanism that doesn't actually exist in the codebase, the agent must surface this rather than silently working around it or making assumptions. Confidence: 0.8
- Prohibits running destructive database commands (schema migrations, table drops, backfills) against non-local/remote databases without explicit per-command confirmation — local dev databases are fine for automated migration. Confidence: 0.9
- Expects pre-migration data integrity checks when schema changes introduce or tighten uniqueness constraints — query existing data for constraint violations before applying the migration, and show the raw results (not just an assertion that it passed). Confidence: 0.8
- During verification/review tasks, when a bug is found the agent must stop, explain what's wrong and what the fix would be, then wait for explicit go-ahead before applying any change — never silently fix-and-continue through the rest of the checklist. Confidence: 0.85
- Prefers semantically meaningful atomic writes (e.g., `increment` on a counter column) over no-op updates for database concurrency control — a real state mutation is unambiguous to every database engine's query planner and cannot be optimized away. Confidence: 0.8
- Prefers database-portable concurrency solutions — locking/serialization logic must be correct under both SQLite and Postgres, not rely on engine-specific transaction behavior (e.g., SQLite's DEFERRED→write-lock upgrade). Confidence: 0.8
- Prefers access control at the route/entry-point level (fail-closed: block the entire route) rather than query-level filtering when a user role has no legitimate reason to access a resource — don't just filter results, prevent the call entirely. Confidence: 0.8
- During multi-tenant or security migrations, latent bugs that become real vulnerabilities due to the change are in-scope for the current pass — the agent must not dismiss them as pre-existing and out of scope. Confidence: 0.8
- Prefers raw SQL (`$queryRaw` with `UPDATE ... RETURNING`) over ORM-level operations for atomic counter increments when the ORM/query-engine may decompose the operation into non-atomic SELECT+UPDATE — correctness over abstraction purity. Confidence: 0.75
- Prefers `$queryRaw` (tagged template with parameterized placeholders) over `$queryRawUnsafe` (string interpolation) for any raw SQL — parameterization is the default even when the interpolated values are trusted internal types like `number` from a session; `$queryRawUnsafe` requires explicit justification. Confidence: 0.85
- When deviating from ORM abstractions to raw SQL for correctness reasons, expects the agent to explicitly acknowledge the tradeoff (lost type safety, bypassed schema-driven query builder) and justify why correctness outweighs abstraction purity in that specific case — don't silently default to raw SQL as if it's an obviously superior choice. Confidence: 0.8
- Prefers empirically testing concurrency — when a fix claims to be race-condition-safe, write a throwaway script that calls the function concurrently (e.g., `Promise.all` x10) and asserts all outputs are unique, run it multiple times, then delete the script. Reasoning alone is insufficient. Confidence: 0.85
- When a dependency/abstraction behaves unexpectedly, digs into its internals (e.g., grepping compiled adapter JS for SQL keywords like `BEGIN`) to understand the actual mechanism rather than guessing from documentation or API surface. Confidence: 0.75
- Expects all features from the base branch (`main`) to be preserved when building a feature branch on top — if something from main is missing after the merge, that's a bug to fix, not an intentional removal. "Nothing from the main should be missed in the new branch. Everything that we built regarding [the feature] is to be on top of main." Confidence: 0.85
- When diagnosing feature regressions after a branch merge, expects systematic `git diff main...featurebranch` comparison to identify what was lost rather than ad-hoc investigation — diff the branch to understand the delta, don't guess. Confidence: 0.8
- Role-based permissions are defined as superset-minus-exceptions: "retains all permissions of [existing role] apart from [specific exclusions]." New roles extend/mirror existing guards (e.g., `requireAdmin`, `resolveStoreId`, inline `=== "superadmin"` checks) rather than having completely independent permission sets enumerated from scratch. Confidence: 0.75
- Expects a clear, documented mapping of test credentials to roles as part of the development environment — when asked "give me the login credentials for each role," expects a table with username, password, role, and store association, not just a verbal summary. Confidence: 0.7
- Expects a documented manual regression testing guide as a standard project artifact — a checklist organized by role and feature area (authentication, CRUD modules, multi-tenant isolation, security boundaries), including login credentials, automated test suite reference, and step-by-step manual verification steps with checkboxes. Stored in `docs/` alongside other project documentation. Confidence: 0.8
